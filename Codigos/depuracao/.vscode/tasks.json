{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "gcc",
      "args": [
        "-g", // Gera informações de depuração
        "${file}",
        "-o",
        "${workspaceFolder}/power" // Nome do executável gerado
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
